import { StyleSheet, View } from "react-native";

type props = {
  id: String;
  showName?: boolean;
};

const ProfileIcon = ({ id, showName }: props) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default ProfileIcon;
